import 'package:flutter/material.dart';

class LocationsList extends StatelessWidget {
  const LocationsList({super.key});

  //static const List<({String name, double lat, double lon})> keyLocations = [(name:'Restaurante Universitário', lat: -23.32538, lon: -51.20182),(name:,lat:, lon: ),(name:,lat:, lon: ),(name:,lat:, lon: ),];

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: ListView(),);
  }
}